package lectures.Semester1.week7;

import java.util.Scanner;

/* @author je-dawson  
   Experiment with reading input files and processing the data.
*/
public class ProcessInput {

  public static void main(String[] args) {
    // // you will need to set some of these variables?!
//    int num, longest = ??, smallest = ??, total = ??, count = ??, average;
//    String word, bigWord = ??;
    
    // 1. Find the smallest number in a file
    // 2. Calculate total of numbers
    // 3. Find words which match a pattern
    // 4. Output numbers which are < (less than) the average
    // 5. Find the longest word


   // Scanner sc = new Scanner(ProcessInput.class.getResourceAsStream("numbers.txt"));
//   Scanner sc = new Scanner(ProcessInput.class.getResourceAsStream("animals.txt"));

//    // 1. find smallest number
//    while (sc.hasNextInt()) {
//      num = sc.nextInt();
//      if (num < smallest) {
//        smallest = num;
//      }     
//    }
//    System.out.println("Small: " + smallest);

// 2. Calculate total of numbers
//    while (sc.hasNextInt()) {
//      num = sc.nextInt();
//      total += num;
//    }
//  System.out.println("total = " + total);

     // 3. Find words which match a pattern
//    while (sc.hasNext()) {
//      word = sc.next();
//      //if ( word.contains("e")) {
//      if ( word.toUpperCase().startsWith("E") && word.length() > 5) {
//        System.out.println(word);
//      }
//    }
//    

//   // 4. Output numbers which are < (less than) average
    //
//    while (sc.hasNextInt()) {
//      num = sc.nextInt();
//      count++;
//      total += num;
//    }
//    
//    average = total/count;
//    System.out.println("Average " + average);
//
//     sc.close(); // reset input file+
//     sc = new Scanner(ProcessInput.class.getResourceAsStream("/resources/numbers.txt"));
//     
//    while (sc.hasNextInt()) {
//      num = sc.nextInt();
//      if (num < average) {
//        System.out.println("Num = " + num);
//      }
//    }


//    // 5. find longest word
//    while (sc.hasNext()) {
//      word = sc.next();
//      //if ( word.contains("e")) {
//      if (word.length() > longest) {
//        bigWord = word;
//        longest = bigWord.length();
//      }
//    }
//    System.out.println("Big word: " + bigWord);

  } // main
} // class
